{
  "name": "verdaccio-test-server",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "verdaccio --config=verdaccio.yaml",
    "verdaccio-login": "NPM_REGISTRY=http://127.0.0.1:5873 npx npm-cli-login -u user -p password -e user@example.com",
    "clean": "rimraf ./storage"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "verdaccio": "^5.26.2"
  }
}
